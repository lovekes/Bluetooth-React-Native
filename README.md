# Bluetooth-React-Native
BLEApp is a React Native app for scanning, connecting to, and reading/writing Bluetooth Low Energy (BLE) devices. It uses a **custom native bridge** (no third-party BLE plugin) so the exact scan, connection, and GATT behavior is fully under our control on both platforms.
